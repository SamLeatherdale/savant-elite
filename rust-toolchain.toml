[toolchain]
# Stable rustc, rustfmt, and clippy. Do not pin nightly or pass `-Z` flags.
#
# rustfmt and clippy stay as explicit components so `cargo fmt` and
# `cargo clippy --all-targets -- -D warnings` match local and CI behavior.
channel = "stable"
components = ["rustfmt", "clippy"]
